"""ReAct tool functions agents use to interact with the world.

Following Manning ch 11.2 — each tool is a @tool-decorated function with a clear
docstring (the docstring becomes the description the LLM sees).
"""
